# github-integ

Fixture repository for Tandem's GitHub context source (PFB-247).

The project source is configured with:

- repository: `Baltaty/github-integ`
- filter term: `(tandem)`

Every commit and pull request here is labelled with what it is meant to
demonstrate, so the Tandem activity feed can be read against this file.
